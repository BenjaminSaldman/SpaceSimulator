<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>NEO Table</title>
  <style>
    /* Add CSS styling for the table */
    table {
      border-collapse: collapse;
      width: 100%;
    }

    th, td {
      border: 1px solid #dddddd;
      text-align: left;
      padding: 8px;
    }

    th {
      background-color: #f2f2f2;
    }
  </style>
</head>
<body>
  <h1>Near-Earth Objects in the Next 24 Hours</h1>
  <table>
    <thead>
      <tr>
        <th>Name</th>
        <th>Estimated Diameter (km) - Min</th>
        <th>Estimated Diameter (km) - Max</th>
        <th>Close Approach Date</th>
      </tr>
    </thead>
    <tbody>
      <% neoData.forEach(neoObject => { %>
        <tr>
          <td><%= neoObject.name %></td>
          <td><%= neoObject.estimatedDiameter.min %> km</td>
          <td><%= neoObject.estimatedDiameter.max %> km</td>
          <td><%= neoObject.closeApproachDate %></td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</body>
</html>
